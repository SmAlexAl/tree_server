README

1) создать .env
2) создать папку storage если ее нет
3) для запуска go run cmd/tree/main.go
4) клиент лежит в папке client - запустить html в браузере

Клиент - 2 дерева данных, снизу находится база данных

ОСОБЕННОСТИ Функционала
1) в скобочках указано текущее состояние объекта - объект мб активный/удален/неизвестное состояние
2) Добавляться в кэш может любой элемент из бд, даже если он был удален
3) Изменяться могут только не удаленные объекты
4) Добавлять элементы можно только, когда состояние родителя активно (то есть нет пометок DELETE или UNKNOWN)
5) Если из БД берется элемент или несколько элементов без привязки к корню, то его состояние будет UNKNOWN
    до те пора, пока из бд не достануться необходимые родительские элементы
    или DELETE - если мы достанем из БД удаленный элемент или удаленного родителя для текущих элементов в кеше